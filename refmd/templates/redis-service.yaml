{{- if .Values.redis.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "refmd.fullname" . }}-redis
  labels:
    {{- include "refmd.componentLabels" (dict "component" "redis" "context" .) | nindent 4 }}
spec:
  type: {{ .Values.redis.service.type }}
  ports:
    - port: {{ .Values.redis.service.port }}
      targetPort: redis
      protocol: TCP
      name: redis
  selector:
    {{- include "refmd.componentLabels" (dict "component" "redis" "context" .) | nindent 4 }}
{{- end }}
